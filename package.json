{
  "name": "tech-z-one",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "python -m ensurepip --upgrade, pip install -y Flask && pip install -y Flask-SQLAlchemy && pip install -y marshmallow && pip install -y Flask-JWT-Extend && pip install -y Flask-CORS &&python3 ./app/__init__.py"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.6.0"
  }
}
